# configs/model/power_net.yaml
_target_: c3jepa_wm.models.power_net.PowerNet # Full absolute python import path

csi_dim: 1
embedding_dim: ${pipeline.msg_embedding_dim}
num_embeddings: ${pipeline.num_embeddings}
max_power: 10
nhead: 4
num_layers: 2
lambda_pow: 0.01
lambda_entropy: 0.05
lambda_critic: 0.1
